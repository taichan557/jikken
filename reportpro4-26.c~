#include<stdio.h>
#include<math.h>


int main(){
  int i,n,k;
  int ans,del;
  int input;
  int left,right,sum;

  scanf("%d",m);

  del=log10()
  
  for (i=0,i<m-1,i++){
    left=left+((xn*cos(2*(M_PI)*k*n))/m);
    right=right+((xn*sin(2*(M_PI)*k*n))/m);
  }
  lefr=left*left;
  right=right*right;

  sum=left+right
  
    
}
